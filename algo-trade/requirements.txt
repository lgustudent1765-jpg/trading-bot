# file: requirements.txt
# Core async runtime
aiohttp==3.9.5

# Data processing
numpy==2.2.4

# Configuration
PyYAML==6.0.1

# Testing
pytest==8.2.2
pytest-asyncio==0.23.7
pytest-cov==5.0.0
pytest-timeout>=2.4.0
pytest-xdist>=3.8.0

# Debugging
rich>=13.0
icecream>=2.1
debugpy>=1.8

# Code quality
mypy==1.10.0
flake8==7.0.0
black==24.4.2
isort==5.13.2
pre-commit>=3.7.0
radon>=6.0          # cyclomatic complexity
semgrep>=1.0        # deep static analysis

# Security
bandit>=1.9         # Python security linter
safety>=3.0         # CVE check for dependencies
pip-audit>=2.0      # Google-backed dependency audit

# Config validation
pydantic>=2.0

# Retry logic
tenacity>=8.0

# Documentation
pdoc>=16.0

# Versioning & changelog
commitizen>=4.0

# Mutation testing (Linux/macOS/WSL only — not native Windows)
# mutmut>=3.0

# Optional: Webull broker (comment out if not using)
# webull==0.2.16

# Optional: Robinhood broker (comment out if not using)
# robin-stocks==3.0.6

# Database
sqlalchemy==2.0.30
psycopg2-binary==2.9.9
greenlet==3.0.3

# Type stubs
types-PyYAML==6.0.12.20240311
